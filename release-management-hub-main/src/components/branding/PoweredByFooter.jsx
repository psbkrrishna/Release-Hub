/* Vendored structurally from production's PoweredByFooter.jsx, minus the
   bundled logo image asset we don't have - a text wordmark stands in. */
export function PoweredByFooter() {
  return (
    <footer className="mt-auto flex w-full items-center justify-end px-6 py-4" aria-label="Brand footer">
      <div className="flex items-center gap-2 text-sm leading-none text-[#222222]">
        <span>Powered by</span>
        <span className="font-semibold text-[#0d59a3]">Zwayam</span>
      </div>
    </footer>
  );
}

export default PoweredByFooter;
